image: Ubuntu1804

platform: x64

branches:
  only:
    - master
    - develop

artifacts:
  - path: $(BUILD_PATH)

install:
  - go version
  - go env
  - echo $PATH
  - make vendor

build_script:
  - make build