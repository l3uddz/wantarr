image:
  - Ubuntu1804
  - macOS

platform: x64

branches:
  only:
    - master
    - develop

artifacts:
  - path: 'dist/$(CMD)_$(TARGET)'

install:
  - go version
  - go env

build_script:
  - make build