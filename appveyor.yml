image: Ubuntu1804

platform: x64

branches:
  only:
    - master
    - develop

install:
  - echo %PATH%
  - echo %GOPATH%
  - set PATH=%GOPATH%\bin;c:\go\bin;%PATH%
  - go version
  - go env

build_script:
  - make build-all